export function SiteFooter() {
  return (
    <footer className="mt-16 border-t border-line py-8 text-center font-mono text-xs text-muted">
      ahmad.ali {"//"} designed &amp; built from scratch — hardware to cloud
    </footer>
  );
}
